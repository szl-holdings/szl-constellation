# REVIVAL CHARTER — llm-router returns as a flagship — 2026-09-04

**Directive:** owner ("I want llm-router back — it's a flagship").

## Current state (audited 2026-09-04)

- `szl-holdings/szl-router` — GitHub repo, ARCHIVED (read-only). Archived note:
  "duplicate/hologram; canonical: szl-holdings/a11oy." Doctrine v7 era.
- Estate `llm-router-live` (20 files, LiveOps) — "Live LLM routing surface" —
  present in the 49-estate constellation manifest, no live endpoint.
- No `SZLHOLDINGS/llm-router` Space exists in the 2026-09-04 census.

## Why revival is legitimate (not zombie resurrection)

The 2026-09-04 topology consolidated defense (killinchu), assurance (sentra),
and the vertical fleet — but no surface owns *routing*: which engine, which
model, which vertical answers a request. Every other plane got a command. The
routing plane is the missing flagship. The archive note pointed at a11oy as
canonical because a11oy carries an internal router — that is an organ, not a
flagship surface.

## Charter

**llm-router — the Routing Command.** The governed traffic plane:

- **Route admission** — every request's engine/model/vertical placement is a
  governed decision with a receipt (policy, cost, capability, doctrine fit).
- **Engine registry** — the six engines (vLLM, SGLang, llama.cpp, MLX, TGI,
  Transformers) declared with honest BLOCKED when unconfigured; wired to
  szl-engine-bench measurements when they exist.
- **Model line** — the org's model zoo (SZL-Khipu-1.5B, chaski, WILLAY, the
  kernel line) as routable targets with download/health telemetry.
- **Cheapest-watt placement** — szl-energy-attest integration; placement
  decisions receipted (SkyPilot take, landed).
- **Deny-by-default** — no route, no compute. Λ advisory only.

## Revival sequence

1. **Owner:** unarchive `szl-holdings/szl-router` (Settings → Danger Zone →
   Unarchive). Archived repos are read-only; this one click unblocks the rest.
2. **Lane:** the router flagship repo gets the doctrine battery + the routing
   plane skeleton (FastAPI: /api/router/engines, /api/router/models,
   /api/router/route with admission receipts).
3. **Lane:** SZLHOLDINGS/llm-router Space created as the flagship surface.
4. **Constellation:** `llm-router-live` estate moves from LiveOps archive to the
   live lattice on first green probe.

## Standing rule

Revival is additive and receipted. The archive history stays in git. Nothing
about the a11oy internal router changes — this is the public routing plane.
